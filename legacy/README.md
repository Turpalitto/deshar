# Legacy Web Prototype

Устаревший HTML/JS прототип до Flutter-приложения (`nokhchiin/`).

Не используется в продакшене. Оставлен для справки и сравнения с ранними уроками.

Запуск локально (опционально): откройте `index.html` в браузере через локальный HTTP-сервер.
